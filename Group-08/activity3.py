"""
Activity 3: Odd Multiples Table
Concepts: for loop, the % (modulo) operator, conditions inside a loop, and accumulation

Scenario:
Create a multiplication table for a given number, but only print the
results for odd multipliers (1, 3, 5, 7, 9). Then, calculate the total
of those results.

Requirements:
- Read n (int) using input().
- Use a for loop from 1 to 10.
- Only process odd values of i using:
    i % 2 == 1
- For each odd i, print exactly:
    n x i = <result>
- Do not print anything for even values of i.
- Add only the odd-multiplier results to the total.
- After the loop, print:
    Sum: <total>

Example test case:
    Input:
        5

    Expected Output:
        5 x 1 = 5
        5 x 3 = 15
        5 x 5 = 25
        5 x 7 = 35
        5 x 9 = 45
        Sum: 125

Write your solution below this line.
"""

# TODO: read n from input()

# TODO: use a for loop (i from 1 to 10) with an if check for odd i,
#       print the line, and accumulate the sum for odd i only

# TODO: print the Sum line
